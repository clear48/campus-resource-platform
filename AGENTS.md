# AGENTS.md

## 项目协作规则

- 默认使用中文沟通和总结。
- 开发前先确认当前分支、`git status` 和最近提交，避免把他人的未提交修改混入本次任务。
- 当前项目默认在 `dev` 分支进行迭代开发。
- `main` 只保存已经验证、可长期运行的稳定版本。
- 未稳定的小功能不得直接提交或推送到 `main`。
- 更完整的项目背景、模块规范、接口规范和文档规范见 `docs/AGENTS.md`。

## 项目技术问答自动归档规则

- 每次处理用户消息时，先读取 `docs/TECHNICAL_QA_LOG.md` 顶部的“归档状态”；状态为 `ENABLED` 时执行归档，状态为 `DISABLED` 时停止归档。
- 用户可随时用自然语言要求“开启/恢复技术问答归档”或“关闭/暂停技术问答归档”；收到后必须立即修改“归档状态”，开关指令本身不写入问答日志。
- 归档处于 `ENABLED` 时，用户每次提出与本项目有关的技术问题，都必须在当前轮最终答复前，将问题和对应答案追加到 `docs/TECHNICAL_QA_LOG.md`。
- 该规则适用于代码解释、架构与设计、接口、数据库、Redis、事务、并发、安全、测试、部署、故障排查、代码审查及实现方案等问题；即使本轮只是只读分析、不修改业务代码，也必须记录。
- 一条消息包含多个独立技术问题时，必须逐项记录问题和答案，不得遗漏；追问需要保留当前问题原文，并补充理解答案所需的最少上下文。
- 记录内容必须与最终答复的技术结论一致；完成工具检查或代码验证后再写入，不记录尚未验证的临时猜测。
- 问答日志只允许追加，不得改写或删除历史记录；旧答案需要纠正时，应新增“更正”记录并引用原记录日期。
- 密码、Token、密钥、数据库凭据、个人信息等敏感内容不得原样写入日志，统一使用脱敏占位符。
- 若因文件只读、冲突或其他原因无法完成归档，必须在当轮最终答复中明确说明，不能静默跳过。

## 模块小功能提交推送规则

- 开发模块时，必须把模块拆分为可以独立验证的小功能。
- 每完成一个模块内小功能，必须先运行对应测试；如果当前无法测试，必须在总结中说明原因。
- 测试通过后，必须立即执行一次 `git add`、`git commit` 和 `git push`，推送到当前开发分支。
- 当前开发分支通常是 `dev` 或 `feature/模块名`。
- 每次任务结束时，必须说明当前分支、测试命令与结果、实际 commit id，以及是否已推送到远程分支。

## 多 Agent 协作入口

- 用户明确要求多 Agent，或任务涉及完整业务模块、数据库/Redis、事务、并发、安全以及跨 3 个以上强关联文件时，主 Agent 应按以下配置调度项目级 Subagent：
  - Codex 环境：`docs/08-multi-agent-collaboration.md` + `.codex/agents/*.toml`
  - Claude Code 环境：`.claude/skills/multi-agent.md`（技能）+ `.claude/agents/*.md`（Agent 定义）
  - 迁移说明见 `docs/09-codex-to-claude-code-migration.md`
- 所有 Subagent 必须遵守本文件与 `docs/AGENTS.md`；主 Agent 对最终范围、代码、测试、提交和推送负责。
- 同一时刻只允许一个 Agent 写入生产代码；只读分析和审查可以并行，测试 Agent 原则上只写测试目录。
- Subagent 共享当前工作区，不等同于独立 Git 分支；两个独立功能确需并行写入时使用独立 Worktree。
- 多 Agent 规则不得覆盖下方前端自动任务队列的范围和停止条件，也不得借委派扩大用户要求。

## 前端代码自动任务推进规则

本规则**只适用于 `frontend/` 目录下的前端代码开发任务**，不适用于后端 Java 代码、SQL、Redis、其他业务模块或纯文档任务。

当用户明确要求“按前端任务队列自动推进”时，必须遵守以下规则：

1. 先读取当前模块的 task queue 文件。
2. 找到第一个状态为 TODO 的任务。
3. 只执行该任务，不主动扩大范围。
4. 完成后运行必要的编译、测试或静态检查。
5. 测试通过后，将任务状态改为 DONE。
6. 更新当前模块开发流程文档。
7. 更新 `docs/CURRENT_STATUS.md`。
8. 更新 `docs/BRANCH_HANDOFF.md`。
9. 生成 Git commit message。
10. 然后继续查找下一个 TODO 任务。
11. 如果遇到停止条件，必须暂停并向用户报告，不允许继续。

停止条件包括：

- 需要新增依赖；
- 需要修改数据库表结构；
- 需要修改其他模块；
- 编译失败；
- 测试失败；
- 业务规则不明确；
- 文档和代码冲突；
- 涉及权限、安全、事务边界不明确。

如果前端任务需要修改后端代码、数据库、Redis 或其他非前端业务模块，必须立即停止并向用户报告，不得把本规则扩展到这些模块。

每个子任务完成后都必须输出：

- 当前完成的任务编号；
- 修改文件；
- 核心逻辑；
- 测试结果；
- 文档更新情况；
- 下一个任务编号；
- 是否继续自动执行。
