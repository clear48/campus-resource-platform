<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.john.campus.mapper.DownloadDeltaSyncItemMapper">

    <!-- 唯一键冲突说明同一 Redis UUID 批次已成功落库，重试时必须跳过 download_count 累加。 -->
    <insert id="insertIgnore">
        INSERT IGNORE INTO download_delta_sync_item (batch_id, resource_id, delta)
        VALUES (#{batchId}, #{resourceId}, #{delta})
    </insert>

    <!-- 幂等命中后回读原始增量，拒绝同一批次/资料 ID 下不一致的异常数据。 -->
    <select id="selectDelta" resultType="long">
        SELECT delta
        FROM download_delta_sync_item
        WHERE batch_id = #{batchId}
          AND resource_id = #{resourceId}
        LIMIT 1
    </select>

    <!-- 只有 Redis 成功 HDEL 后才记录确认时间；未确认记录保留，确保故障重试仍可识别已落库批次。 -->
    <update id="markConfirmed">
        UPDATE download_delta_sync_item
        SET confirmed_at = #{confirmedAt}
        WHERE batch_id = #{batchId}
          AND resource_id IN
        <foreach collection="resourceIds" item="resourceId" open="(" separator="," close=")">
            #{resourceId}
        </foreach>
    </update>

</mapper>
