<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.john.campus.mapper.UserFileAuthorizationMapper">

    <!-- 授权关系以用户和文件唯一；只返回布尔值，不暴露其他用户信息。 -->
    <select id="exists" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM user_file_authorization
            WHERE user_id = #{userId}
              AND file_id = #{fileId}
        )
    </select>

    <!-- 并发上传相同内容时允许重复尝试授权，由唯一索引保证最终只有一条记录。 -->
    <insert id="insertIgnore">
        INSERT IGNORE INTO user_file_authorization (user_id, file_id, source_type)
        VALUES (#{userId}, #{fileId}, #{sourceType})
    </insert>

</mapper>
